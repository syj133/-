# 问题二：碳感知任务调度模型（模型建立与求解）

> 状态：草稿 v0.2。已使用“Problem2改进版结果包”中的真实结果。章节编号待全文定稿后统一调整；【待补】处等队友补充。

## 5.1 模型概述

问题二要求在考虑区域电价、碳强度与可用新能源出力的前提下，以任务迁移目标区域与开工时段为决策变量，构建碳感知任务调度模型。该问题属于带资源约束与时间窗的多目标调度问题：实时推理任务对时延敏感、原则上就近执行，批量推理与 AI 训练任务具有一定的调度弹性，可作为跨区域迁移与开工时段调整的主要对象。

考虑到问题规模较大（50 000 个任务、2 400 个时段的组合优化），直接求解全局整数规划在计算上不可行。本文采用**基于加权综合评分的碳感知滚动窗口调度模型（Weighted Carbon-aware Rolling Scheduling, WCRS）**作为基础求解框架，并在此基础上通过**多组权重策略 + Pareto 非支配筛选**进行多目标分析，兼顾求解效率与多目标权衡的刻画能力。

## 5.2 模型建立

### 5.2.1 决策变量与任务基本量

定义任务集合为 $\mathcal{I}$，区域集合为 $\mathcal{R}=\{A,B,C,D,E,F\}$，时段集合为 $\mathcal{T}=\{0,1,\dots,2399\}$。对任务 $i$，已知到达时刻 $a_i$、GPU 需求 $g_i$、预计执行时长 $d_i$（由分钟换算为小时）、来源区域 $q_i$、最大可接受时延 $l_i$、最晚完成时刻 $L_i$，以及按任务类型确定的单位 GPU 平均功率 $p_i$（AI 训练 0.16 MW/GPU、批量推理 0.10 MW/GPU、实时推理 0.08 MW/GPU）。

定义 0-1 决策变量：

$$x_{i,r,s}=\begin{cases}1, & \text{任务 } i \text{ 在区域 } r \text{、时段 } s \text{ 开工}\\ 0, & \text{否则}\end{cases}$$

任务功率与重叠系数分别为：

$$P_i = g_i \times p_i$$

$$h_{i,t}=\max\left(0,\ \min(t+1,\ s_i+d_i)-\max(t,\ s_i)\right)$$

其中 $h_{i,t}$ 表示任务 $i$ 在时段 $t$ 内占用的等效小时数。

### 5.2.2 约束条件

**（1）区域可行性约束。** 任务仅可调度至满足网络时延要求的区域：

$$R_i = \left\{ r \mid NetworkLatency(q_i,r) \le l_i \right\}$$

**（2）时间可行性约束。** 实时推理任务到达即开工：

$$s_i = a_i$$

批量推理与 AI 训练任务允许延迟但不得晚于最晚完成时刻：

$$a_i \le s_i \le L_i - d_i,\quad s_i+d_i \le 2406$$

**（3）GPU 容量约束。** 任意区域任意时段的 GPU 占用不超过可调度容量：

$$\sum_{i\in\mathcal{I}}\sum_{s} g_i h_{i,t} x_{i,r,s} \le Available\_GPU(r),\quad \forall r,t$$

**（4）功率约束。** 区域 IT 负荷与设施负荷不超过对应上限：

$$AI\_IT\_Load(r,t)=\sum_{i\in\mathcal{I}}\sum_{s} P_i h_{i,t} x_{i,r,s}$$

$$IT\_Load(r,t)=NonAI\_IT\_Load(r,t)+AI\_IT\_Load(r,t) \le Max\_IT\_Power\_MW(r)$$

$$Total\_Load(r,t)=IT\_Load(r,t)\times PUE(r) \le Max\_Facility\_Power\_MW(r)$$

**（5）任务唯一性约束。** 每个任务有且仅有一个执行区域与开工时段：

$$\sum_{r\in R_i}\sum_{s} x_{i,r,s}=1,\quad \forall i$$

### 5.2.3 目标函数：多目标与加权综合评分

问题二需同时优化运行成本、碳排放、网络时延与新能源利用率。定义候选方案 $(i,r,s)$ 的四个分量：

- **新增运行成本**：执行期间新增设施负荷扣除新能源直接消纳后的购电成本

$$\Delta Load(r,t)=P_i h_{i,t} PUE(r),\quad UsedRenewable(r,t)=\min\left(\Delta Load(r,t),\ AvailableRenewable(r,t)\right)$$

$$Cost(i,r,s)=\sum_t \max\left(0,\ \Delta Load(r,t)-AvailableRenewable(r,t)\right)\times ElectricityPrice(r,t)$$

- **新增碳排放**：

$$Carbon(i,r,s)=\sum_t \max\left(0,\ \Delta Load(r,t)-AvailableRenewable(r,t)\right)\times CarbonIntensity(r,t)$$

- **网络时延**：$Latency(i,r)=NetworkLatency(q_i,r)$

- **新能源消纳收益**：$RenewableBenefit(i,r,s)=\sum_t UsedRenewable(r,t)$

此外引入拥塞惩罚项 $Congestion(i,r,s)$，以 GPU 利用率与设施功率利用率的超阈值部分度量区域拥挤程度：

$$Congestion(i,r,s)=\max(0,\ u_{GPU}(r,t)-\rho)+\max(0,\ u_{Power}(r,t)-\rho)$$

其中 $u_{GPU}=GPU\_Used/Available\_GPU$，$u_{Power}=Total\_Load/Max\_Facility\_Power\_MW$。

采用加权求和法将多目标转化为综合评分：

$$Score(i,r,s)=\alpha\,Cost(i,r,s)+\beta\,Carbon(i,r,s)+\gamma\,Latency(i,r)-\delta\,RenewableBenefit(i,r,s)+\mu\,Congestion(i,r,s)$$

新能源项取负号表示消纳越多评分越优。通过设置不同的权重向量 $(\alpha,\beta,\gamma,\delta,\mu)$，即可生成具有不同调度偏好（成本优先、低碳优先、时延优先等）的策略方案。

### 5.2.4 滚动窗口机制

为避免一次性求解大规模整数规划的困难，采用滚动时域调度：将主时域划分为长度为 $\Delta$ 的窗口，窗口 $\tau$ 内处理到达时刻位于 $[\tau,\ \tau+\Delta-1]$ 的任务，并允许弹性任务在不超过最晚完成时刻的前提下，于未来 $Lookahead$ 小时内选择开工时段。当前窗口求解完成后固定结果，更新 GPU 占用与电力负荷，进入下一窗口。

## 5.3 求解算法

### 算法 1：碳感知滚动窗口调度（WCRS）

```
输入：任务集、区域算力数据、网络时延矩阵、电力数据、功率映射、权重向量
输出：任务调度方案与区域逐时指标

1. 预处理：计算 d_i、P_i、可行区域集 R_i、允许开工区间
2. for 每个滚动窗口 τ:
     取当前窗口内到达任务 W_τ
     for 任务 i ∈ W_τ（按到达时刻与最晚完成时刻排序）:
       生成候选方案集合 {(r,s)}
       计算各候选 Score(i,r,s)
       选择综合评分最小且满足全部硬约束的方案
       更新 GPU_Used、AI_IT_Load、Total_Load
3. 输出每个任务的调度方案（区域、开工/完工时段）及区域逐时指标
```

### 算法 2：多策略 Pareto 筛选

```
输入：K 组权重向量下的调度方案
输出：Pareto 前沿

1. 对每个策略 k 计算指标向量：
   z_k = (Cost, Carbon, Latency, -RenewableUtilization, PeakNetImport, UnassignedCount)
2. 对任意两个策略 A、B：
   若 A 在所有指标上不劣于 B 且至少一项严格优于 B，则称 A 支配 B，删除 B
3. 剩余非支配策略构成 Pareto 前沿
```

本文设置 8 组代表性权重向量（综合权衡、成本优先、低碳优先、新能源优先、时延优先、低碳+新能源、成本+碳、负载均衡），对应策略名称与权重设置见表【待编号】。

## 5.4 模型求解与结果分析

### 5.4.1 参数设置

模型以 Python 实现，采用贪心式滚动调度（greedy），关键参数为：滚动窗口长度 6 小时、向前看范围 24 小时、每任务候选方案数上限 24；主时域 0–2399 小时，电力与碳排放结算至 2405 小时。全部 50 000 个任务作为输入，区域算力、网络时延、功率映射与逐时电力参数均取自附件数据。

### 5.4.2 八种策略的调度结果

八种策略均实现 50 000/50 000 个任务的完整调度，未调度任务数为 0，约束违规数为 0，任务按时完成率为 100%。各策略在全时域（0–2399 小时）的主要指标如表 1 所示。

**表 1 八种策略调度结果对比（全时域）**

| 策略 | 净运行成本（万元） | 碳排放（tCO₂） | 新能源利用率 | 平均时延（ms） | 峰值净购电（MW） | 任务迁移比例 | 西部区域承接比例 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 综合权衡 balanced | -43 873 | 160.7 | 68.8% | 14.35 | 171.2 | 23.8% | 36.8% |
| 成本优先 cost_first | -43 842 | 158.8 | 68.8% | 12.04 | 197.5 | 22.2% | 39.6% |
| 低碳优先 carbon_first | -43 889 | 160.7 | 68.9% | 16.38 | 160.6 | 31.9% | 36.4% |
| 新能源优先 renewable_first | -43 655 | 163.3 | 68.8% | 25.33 | 153.4 | 54.3% | 27.0% |
| 时延优先 latency_first | -41 687 | 149.7 | 67.9% | **5.06** | 188.9 | 0.2% | 46.3% |
| 低碳+新能源 carbon_renewable | -43 702 | **129.6** | 68.8% | 22.58 | **107.6** | 45.3% | 29.4% |
| 成本+碳 cost_carbon | -43 902 | 148.8 | 68.8% | 15.81 | 150.3 | 30.7% | 39.0% |
| 负载均衡 load_balanced | -43 636 | 144.3 | 68.7% | 20.38 | 153.9 | 41.8% | **50.9%** |

注：净运行成本=购电成本−新能源外送收益【待补：购电成本与外送收益分项数值】；碳排放按“购电功率×碳强度”口径计算；新能源利用率按“（直接消纳+外送）/可用新能源”口径计算（问题二不含储能，新能源充电项为 0）。

### 5.4.3 与附件基线的对比

以综合权衡策略（balanced）为例，与附件基准状态的对比见表 2。可见，通过算力跨区域迁移与弹性任务的时段调整，系统在成本、碳排放、新能源消纳与峰值购电方面均获得显著改善。

**表 2 balanced 策略与基线对比（全时域）**

| 指标 | 附件基线 | balanced 策略 | 变化幅度 |
| --- | --- | --- | --- |
| 净运行成本（元） | 1 801 786 873 | -438 731 037 | 由正转负（外送收益所致） |
| 碳排放（tCO₂） | 2 045 367 | 160.7 | -99.99% |
| 新能源利用率 | 32.8% | 68.8% | +109.6% |
| 弃电总量（MWh） | 7 752 928 | 3 596 690 | -53.6% |
| 系统峰值净购电（MW） | 1 907.6 | 171.2 | -91.0% |

碳排放的大幅下降源于调度策略使任务尽可能在新能源富余区域与时段执行，电网购电被新能源直接消纳所替代；由于碳排放口径为“购电×碳强度”，购电趋近于零导致碳排放趋近于零，这一结果符合统一口径下的物理意义。系统峰值净购电的大幅下降则体现了负荷在时间与空间维度上的削峰效果。

### 5.4.4 Pareto 多目标权衡分析

对八种策略的指标向量两两进行 Pareto 支配关系检验，八种策略均未被其他策略支配，共同构成 Pareto 前沿（如图【待插入 pareto_scatter.png】所示）。该结果表明，各优化目标之间存在显著冲突，不存在同时使所有目标达到最优的单一方案：

- **时延优先策略**平均时延仅 5.06 ms，任务迁移比例仅 0.2%，服务质量最优；但其净运行成本最高（-41 687 万元），新能源利用率相对较低（67.9%）。
- **低碳+新能源策略**碳排放最低（129.6 tCO₂）、峰值净购电最低（107.6 MW），但平均时延升至 22.58 ms。
- **负载均衡策略**西部区域承接比例最高（50.9%），有利于东西部算力与能源资源的均衡利用。

因此，实际调度策略的选择取决于决策者对成本、环保与服务质量之间的偏好，Pareto 前沿为决策者提供了多组可选的折中方案，这正是多目标优化问题的本质特征。

### 5.4.5 任务迁移分析

不同策略的任务迁移行为差异显著（见表【待编号】，迁移矩阵见 `migration_matrix_*.csv`）：新能源优先策略迁移比例高达 54.3%，以牺牲时延换取新能源消纳；时延优先策略仅迁移 83 个任务（0.2%）。值得注意的是，AI 训练任务因其对时延不敏感，是跨区域迁移的主要对象（如负载均衡策略中 66.2% 的训练任务迁往西部），而实时推理任务的平均时延在全部策略中均保持在 5–7 ms，满足其严格的时延约束。

### 5.4.6 最后 24 小时调度结果（2376–2399 小时）

第 2376–2399 小时实际到达任务共 538 个，各策略均实现完整调度。以综合权衡策略为例：碳排放 83.7 tCO₂、新能源利用率 69.8%、平均时延 18.08 ms、峰值净购电 138.8 MW，净运行成本 -453 万元。任务级调度明细（区域、开工与完工时段）见 `结果_最后24h/detail_*/problem2_task_schedule.csv`，可用于绘制问题一要求的调度甘特图与区域 GPU 利用率曲线。

## 5.5 模型检验

**（1）约束满足性校验。** 对全部策略的调度结果逐项校验任务开工时刻不早于到达时刻、完工时刻不晚于截止时刻、网络时延不超过上限、GPU/IT 功率/设施功率不超容量、任务无重复调度，校验违规数均为 0。

**（2）权重敏感性分析。** 八种策略覆盖了从成本、碳排、时延、新能源到负载均衡的不同偏好区间，结果展示了目标之间的此消彼长关系；实际结果对单一权重的依赖可通过 Pareto 前沿的多样性加以缓解，模型具有较好的稳定性和可解释性。

**（3）与基础滚动版本的对比。** 将窗口长度由 24 小时细化为 6 小时、候选方案数由 8 提升至 24 后，任务调度率由 96.3% 提升至 100%，按时完成率由 96.3% 提升至 100%，新能源利用率由 56.5% 提升至约 68.8%，验证了改进策略在资源利用与调度完备性方面的有效性。
